# .dockerignore 文件的作用类似于 .gitignore。
# 它告诉 Docker 在构建镜像时，哪些文件或文件夹“不需要”搬进容器里。

# 1. 忽略版本控制系统 (Git) 相关文件
# 意义：镜像只需要运行代码，不需要完整的 Git 历史（这可能非常大）。
.git
.gitignore

# 2. 忽略 Python 编译产生的缓存文件
# 意义：__pycache__ 文件夹里存的是字节码文件（.pyc），它们是根据你的宿主机环境生成的。
# 搬进容器后可能因为系统不一致导致运行异常，且会让镜像变臃肿。
**/__pycache__/
*.py[cod]
*$py.class

# 3. 忽略各种虚拟环境文件夹
# 意义：末尾带 / 表示忽略整个文件夹。
# 虚拟环境（如 venv/）包含了成千上万个针对你本地电脑安装的库文件。
# 我们在 Dockerfile 里已经通过 pip install 安装了所需环境，所以绝对不能把本地环境搬进去，否则会造成版本冲突。
venv/
.venv/
env/
conda-env/

# 4. 忽略 IDE（集成开发环境）的私有配置
# 意义：这些是 VS Code 或 PyCharm 产生的本地配置（如断点、窗口布局等），容器运行完全不需要。
.vscode/
.idea/
*.swp
*.swo

# 5. 忽略大型模型权重、数据集和训练输出
# 意义：深度学习的模型文件（.pth, .bin）通常很大（几百 MB 到几 GB）。
# 它们应该通过“目录挂载 (Volumes)”的方式在运行时进入容器，而不是直接打进镜像里。
# 否则，你每改一行代码，Docker 都要重新打包这几 GB 的大文件，速度会慢死。
*.pth
*.bin
*.checkpoint
out/
data/

# 6. 忽略日志文件
# 意义：防止本地产生的错误日志被带进镜像。
*.log
